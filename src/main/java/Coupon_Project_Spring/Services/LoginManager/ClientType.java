package Coupon_Project_Spring.Services.LoginManager;

/**
 * Enum for the different types of clients that can log in to the system.
 */
public enum ClientType {
    Administrator, Company, Customer
}
